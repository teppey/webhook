#!/usr/bin/env groovy

pipeline {
    agent any
    stages {
        stage('Check') {
            echo 'Check'
        }
        stage('Test') {
            steps {
                echo 'Hello world'
            }
        }
    }
}
